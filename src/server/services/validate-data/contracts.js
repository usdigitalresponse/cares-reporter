const { ValidationItem } = require("../../lib/validation-log");

const validateContracts = (documents = [], subrecipientsHash) => {
  const valog = [];

  documents.forEach(({ content }, row) => {});
  return valog;
};

module.exports = validateContracts;
